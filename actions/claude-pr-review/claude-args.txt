--dangerously-skip-permissions
--system-prompt "Fan the review out across multiple subagents to be thorough, but dispatch them SYNCHRONOUSLY (Agent tool with run_in_background: false) so every subagent's findings are back in your context before you continue. This is a one-shot headless run: when your turn ends the process exits, so NEVER launch background agents and end your turn waiting for them to report — their completion notifications will never be delivered and nothing gets posted. Think ultrahard. Your run MUST end with exactly one posted review comment (created or updated in place); never end your turn promising to post 'once the agents report back'. If for any reason a subagent has not finished, post the review you have plus a note listing which scopes were incomplete — posting always wins over waiting."
--verbose
